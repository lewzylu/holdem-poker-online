## 1. 桌面形状与画布骨架

- [x] 1.1 在 `public/css/table.css` 中把 `.felt` 由 `border-radius: 50%` 改为圆角长方形（画布内居中 900×400、圆角 120px），边框改为细金线；验证：桌面呈现上下水平直边与左右竖直直边，`node tools/render-check.js` 通过
- [x] 1.2 让座位容器 `#seats` 直接基于画布坐标系定位（脱离 `.felt` 的 padding box），并移除 `public/js/table.js` 中 `FELT_BORDER` / `SEATS_W` / `SEATS_H` 这组因边框差异而存在的常量；验证：全仓搜索 `FELT_BORDER` 零命中，座位定位不再受 `.felt` 边框宽度影响

## 2. 九个固定槽位

- [x] 2.1 在 `public/js/table.js` 中新增九项槽位坐标常量表（ME 600,500；S1 430,500；S2 770,500；S6 500,100；S5 700,100；S7 150,220；S8 150,380；S4 1050,220；S3 1050,380），并注明「改动必须重跑 layout-test，底边最紧余量仅 30px」；验证：常量被 `renderSeats()` 引用，无遗留未使用项
- [x] 2.2 删除 `seatPos()` 中的椭圆参数方程与 `ELLIPSE_RX` / `ELLIPSE_RY` / `MY_ANGLE` 常量，改为按槽位表查表；验证：全仓搜索 `ELLIPSE_RX`、`MY_ANGLE` 零命中（测试脚本除外，第 6 组处理）
- [x] 2.3 改写 `renderSeats()`：去掉 `filter(s => s.name)`，改为遍历全部九个槽位；在座玩家按「自本人起顺时针」映射到槽位，本人固定落在 ME；验证：3 人在座时页面渲染出 9 个座位节点，本人位于底边正中
- [x] 2.4 空槽位渲染为虚线占位，不含昵称、筹码、底牌、下注额与动作反馈，且不绑定任何点击处理；验证：点击空位无任何状态变更，空位节点内无玩家信息元素

## 3. 座位卡与浮层

- [x] 3.1 在 `public/css/table.css` 中把座位改为卡片式：固定 140×56，内部为头像 + 昵称 + 筹码横排，深色半透明底 + 细边；验证：任意牌局阶段下座位卡实测尺寸恒为 140×56
- [x] 3.2 底牌改为卡片上方的绝对定位浮层（向上探出 40px），牌面尺寸沿用现有 `.card.small` 不变；验证：本人底牌与对手牌背尺寸严格相等，且底牌不参与座位卡的布局计算
      - 实施修正：牌统一朝**桌心**方向探出（下半部槽位朝上、上半部 `.flip` 朝下），否则顶边席位的牌会顶向画布外缘。碰撞盒随之翻转，仍为 140×96
- [x] 3.3 动作反馈浮层由卡片上方（`bottom: calc(100% + 4px)`）改为卡片下方，确保与底牌区不重叠；验证：某座位同时持牌并显示反馈时，牌在卡上、反馈在卡下，两者矩形无交集
- [x] 3.4 座位状态文字（行动中 / 上一步动作）与下注额的位置随之调整，保持绝对定位、不撑高座位卡；验证：座位卡高度在「无牌 / 有牌 / 有反馈 / 有下注额」四种状态下均为 56
      - 徽章 `.sub` 也一并从「卡片正上方」移到右上角外侧（正上方已归底牌）

## 4. 废除按人数分级降级

- [x] 4.1 删除 `public/js/table.js` 中 `.crowded` 类的切换逻辑；验证：JS 中搜索 `crowded` 零命中
- [x] 4.2 删除 `public/css/app.css` 中挂在 `#seats.crowded` 上的四组规则（隐藏对手昵称、隐藏对手动作文字、隐藏座位秒数、反馈退化为圆点）及关联的 `dot-pop` 动画；验证：全仓搜索 `crowded` 仅剩测试脚本与文档中的历史描述，样式表零命中
- [x] 4.3 确认在座 9 人时昵称、动作文字与座位秒数均正常显示；验证：满席场景下每个在座槽位都能读到昵称，反馈为完整文字而非圆点
      - 实测满席 9 人：9 个昵称齐全、9 条动作文字均为完整文案（「过牌」/「行动中…」）、座位秒数正常显示 30

## 5. 配色

- [x] 5.1 桌布由亮绿径向渐变改为墨绿，边框改为细金线描边；验证：桌面观感与目标一致，且 `.felt` 边框宽度变化不再影响座位定位（依赖 1.2 已解耦）
- [x] 5.2 座位卡、底牌浮层、反馈浮层的配色与深色桌面协调，本人座位保留金色高亮以便一眼定位；验证：本人槽位可与其他槽位区分，空位可与在座槽位区分

## 6. 测试改造

- [x] 6.1 重写 `tools/layout-test.js` 的几何部分：从源码解析槽位常量表，验证①九席两两不重叠（碰撞盒 140×96）②全部落在 1200×600 画布内③全部位于直边区段、无一落在圆角上④左两席同 x、右两席同 x、底三席同 y、顶两席同 y⑤左右镜像；验证：`node tools/layout-test.js` 通过，并打印出最紧一对的余量
      - 实测最紧一对是侧边 `3-4`（余量 24px），而非 design 预估的底边 `0-1`（30px）——原因是牌改朝桌心后侧边上下两席的牌相向而出。已加「余量不低于 10px 安全线」断言
      - 最终定案 `--hole-peek: 22px`：碰撞盒 140×101，最紧一对仍是侧边 `3-4`，余量 14px。取 22 的推导见 `table.css` 注释（`2 × (67 - peek) < 104` ⇒ `peek > 15`）
- [x] 6.2 更新 `tools/layout-test.js` 的页面部分：断言渲染出九个槽位（而非在座人数）、本人位于 ME、渲染 `left/top` 与常量表一致、空位不含玩家信息；验证：脚本通过
- [x] 6.3 更新 `tools/visual-check.js`：移除 `crowded` 相关断言，「座位不重叠」改为对九个槽位判定，新增「底牌在卡上方、反馈在卡下方且互不重叠」断言；验证：脚本在 7 种视口下全部通过
      - 该断言抓到真实回归：上半部（`.flip`）四席的牌朝下、反馈也在卡下方，两者重叠。修复为「反馈永远在牌的反面」（`.seat.flip .act-flash` / `.last` / `.thinking` 翻到卡上方），并补上「上半部槽位的反馈在底牌上方」断言
- [x] 6.4 更新 `tools/e2e-visual.js`：移除 `crowded` 断言，保留跨视口比例恒定与控制台报错检查；验证：脚本通过
- [x] 6.5 跑通 `tools/render-check.js` 与 `tools/nav-test.js`，确认无因 DOM 结构调整而失败；验证：两脚本均退出码 0

## 7. 文档与收尾

- [x] 7.1 改写 `README.md`「固定比例画布」一节：椭圆常量改为槽位表说明，补充圆角长方形选型理由（保留直边以对齐同侧席位）、碰撞盒与持牌解耦、`.crowded` 已废除；验证：README 中不再出现 `ELLIPSE_RX` 与按人数降级的描述
      - 踩坑清单同步补入本轮四条：`#seats` 定位父级、底牌 `z-index` 不能取 -1、底牌靠左偏置、`--hole-peek` 的数学下界与「牌/反馈随 flip 翻面」
- [x] 7.2 端到端验证：在桌面宽屏、桌面窄高、手机横屏三种视口下各跑一手牌，确认九席稳定、空位可见不可点、牌与反馈分处上下、比例不变、无控制台报错；验证：三种视口均符合预期
