## 1. 服务端：扩展下发视图

- [x] 1.1 在每人牌桌视图中把行动截止时间与行动者标识提升为表级字段，验证方式：连接两个客户端，让其中一方处于行动状态，确认**另一方**收到的视图里能取到截止时间与行动者标识（此前仅行动者本人可见）
- [x] 1.2 为玩家动作附加单调递增序号，随视图下发，验证方式：连续做出两次文案完全相同的动作（例如两位玩家都「跟注 50」），确认序号发生了变化
- [x] 1.3 确认无人待行动时（发牌中、结算展示、牌局未开始）截止时间字段为空，验证方式：在这三种状态下各取一次视图，确认字段为空而非保留上一次的旧值
- [x] 1.4 在广播合并处补充注释，说明同 tick 连续动作会被合并、盲注因此不作为需要闪现的动作处理，验证方式：注释已写入且指明该限制是刻意接受的而非缺陷

## 2. 前端：本人倒计时

- [x] 2.1 在计时器容器中增加秒数节点，验证方式：页面结构中存在该节点且默认不可见
- [x] 2.2 渲染整秒剩余时间并与进度条并存，验证方式：轮到自己时能看到整秒数字，且数字不含小数位
- [x] 2.3 实现三阶颜色切换（大于 11 秒 / 8 至 11 秒 / 小于 8 秒），验证方式：观察一次完整的 30 秒倒计时，确认在 11 秒与 7 秒两处发生视觉状态切换
- [x] 2.4 处理归零与提前行动两种收尾，验证方式：放任超时一次确认显示为 `0` 而非负数；另一次提前提交动作确认倒计时立即消失

## 3. 前端：对手行动状态

- [x] 3.1 依据表级截止时间为当前行动者座位渲染视觉状态，验证方式：作为旁观者观察对手行动，确认其座位可被明确识别
- [x] 3.2 让该状态随三阶阈值增强，且与本人倒计时共用同一套阈值，验证方式：同一时刻对照本人数字颜色与对手座位颜色，确认两者阶段一致
- [x] 3.3 处理行动权转移时的状态清除，验证方式：连续观察两位玩家依次行动，确认前一位的行动中标示被清除、不会同时出现两个
- [x] 3.4 处理行动者掉线时同时呈现「等待行动」与「已掉线」两种状态，验证方式：让一名玩家在其行动回合断开连接，确认两种状态都可见

## 4. 前端：动作反馈闪现

- [x] 4.1 实现动作反馈浮层的基础样式：绝对定位、不参与布局、不接收指针事件，验证方式：反馈显示时座位位置与尺寸无位移，且点击可穿透到下方元素
- [x] 4.2 基于动作序号变化触发反馈，验证方式：对手做出动作后反馈出现；重复推送同一状态时不重复触发
- [x] 4.3 实现驻留 3 秒后消失，验证方式：计时确认约 3 秒后消失，且座位的筹码、底牌、下注额不受影响
- [x] 4.4 处理同一玩家连续行动时的内容更新与计时重置，验证方式：在前一次反馈未消失时再次行动，确认内容更新为最新动作且重新计时
- [x] 4.5 覆盖零金额动作（过牌、弃牌）的反馈，验证方式：分别做出过牌与弃牌，确认两者都产生可见反馈
- [x] 4.6 覆盖超时自动处置的反馈，并使其可与主动动作相区分，验证方式：放任一次超时，确认反馈出现且能看出是自动处置
- [x] 4.7 一手牌结束时清除所有驻留中的反馈，验证方式：一手牌结束后确认所有座位上无残留反馈
- [x] 4.8 为本人动作提供确认反馈，验证方式：自己提交加注后在自己座位处看到反馈；构造一次被服务端拒绝的动作，确认不出现成功反馈而是收到错误提示

## 5. 前端：解除隐藏与分级降级

- [x] 5.1 解除样式中对非本人座位动作文字的全局隐藏，验证方式：少人局下对手动作文字可见
- [x] 5.2 实现按入座人数（7 人及以上）切换降级呈现，验证方式：分别在 4 人与 8 人局下观察，确认少人局显示文字、满桌降级为不依赖文字的形式
- [x] 5.3 处理入座人数跨越阈值时的形式切换，验证方式：从 6 人增至 7 人，确认呈现形式随之切换
- [x] 5.4 确认动效使用不触发重排的属性，验证方式：动效运行期间座位几何不发生变化

## 6. 回归验证

- [x] 6.1 运行 `npm run test:edge`，确认超时自动处置的断言仍然通过（本次不改超时时长与默认动作，此断言是防止误伤规则的护栏）
- [x] 6.2 运行 `npm test` 与 9 人满桌压测，确认资产守恒断言通过、无服务端错误
- [x] 6.3 运行 `npm run test:layout`，确认新增浮层未破坏座位防重叠计算
- [x] 6.4 在 4 人与 9 人两种人数下，分别于宽屏与手机横屏尺寸实测，确认无溢出、无重叠、反馈不遮挡本人底牌
- [x] 6.5 验证向后兼容：服务端更新而前端未更新时功能退化为当前行为、不报错，验证方式：用旧前端连接新服务端完成一手牌
